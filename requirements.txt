fastapi>=0.118.0
uvicorn>=0.37.0
sqlalchemy>=2.0.41
psycopg2-binary>=2.9.11
python-dotenv>=1.1.0
groq>=0.31.0
pydantic>=2.12.0
requests>=2.32.3
httpx>=0.28.0
pypdf>=5.0.0
torch>=2.0.0
torchvision>=0.15.0
pillow>=10.0.0
python-multipart>=0.0.9
sentence-transformers>=2.7.0
numpy>=1.26.0
rembg>=2.0.50
# CPU-only by default. On a machine with an NVIDIA GPU + CUDA installed,
# swap this for `onnxruntime-gpu>=1.16.0` (uninstall onnxruntime first —
# the two packages conflict) so rembg's background remover picks up
# CUDAExecutionProvider automatically; see ml_model.py's _get_bg_session().
onnxruntime>=1.16.0